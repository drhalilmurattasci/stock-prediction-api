# Application settings example (non-secret). Secrets belong in .env, not here.
app:
  env: local
  log_level: INFO
forecasting:
  default_model: chronos        # baselines | chronos | nixtla | gboost
  horizons: [1, 5, 21]
  intervals: [0.5, 0.8, 0.95]
