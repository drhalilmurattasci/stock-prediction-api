# alembic.ini — PLACEHOLDER
# TODO: run `alembic init migrations` to generate the real config,
# then set sqlalchemy.url from DATABASE_URL (.env).
[alembic]
script_location = migrations
# sqlalchemy.url =  # injected from app config at runtime
